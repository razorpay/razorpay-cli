name: Docker Build and Push Devstack

on:
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  REGISTRY: c.rzp.io
  REPO_PREFIX: fnd

jobs:
  # =============================================================================
  # Build: Build and push architecture-specific devstack images
  # =============================================================================
  # Runs jobs in parallel (2 platforms × N binaries)
  build:
    strategy:
      fail-fast: false
      matrix:
        platform:
          - linux-amd64
          - linux-arm64
        binary:
          - user
    name: Build ${{ matrix.binary }} (${{ matrix.platform }})
    runs-on:
      - non-api
      - ${{ matrix.platform }}
    steps:
      - name: Generate GitHub Token
        uses: actions/create-github-app-token@67018539274d69449ef7c02e8e71183d1719ab42
        id: app-token
        with:
          app-id: ${{ secrets.WORKFLOWGATEKEEPER_APP_ID }}
          private-key: ${{ secrets.WORKFLOWGATEKEEPER_PRIVATEKEY }}
          owner: "razorpay"

      - name: Build and Push Image
        uses: razorpay/actions/docker-image-build-push@master
        with:
          username: ${{ secrets.HARBOR_DOCKER_USERNAME }}
          password: ${{ secrets.HARBOR_DOCKER_PASSWORD }}
          registry: ${{ env.REGISTRY }}
          repository: ${{ env.REPO_PREFIX }}-${{ matrix.binary }}
          enable-login: true
          push: true
          platforms: ${{ matrix.platform == 'linux-amd64' && 'linux/amd64' || 'linux/arm64' }}
          # Use devstack tag with arch suffix
          custom-tag: devstack-${{ matrix.platform == 'linux-amd64' && 'amd64' || 'arm64' }}
          file: Dockerfile.devstack
          build-args: |
            REGISTRY=${{ env.REGISTRY }}/razorpay
            ARG_BIN=${{ matrix.binary }}
          secrets: |
            git_token=${{ steps.app-token.outputs.token }}
          full-setup: true

  # =============================================================================
  # Create Manifest: Combine arch-specific images into multiarch manifest
  # =============================================================================
  # Runs jobs in parallel (1 per binary) after all builds complete
  create-manifest:
    needs: [build]
    strategy:
      fail-fast: false
      matrix:
        binary:
          - user
    name: Create Manifest ${{ matrix.binary }}
    runs-on:
      - non-api
      - X64
    steps:
      - name: Create Multiarch Manifest
        uses: razorpay/actions/docker-image-build-push@master
        with:
          username: ${{ secrets.HARBOR_DOCKER_USERNAME }}
          password: ${{ secrets.HARBOR_DOCKER_PASSWORD }}
          registry: ${{ env.REGISTRY }}
          repository: ${{ env.REPO_PREFIX }}-${{ matrix.binary }}
          enable-login: true
          # Final manifest tag
          custom-tag: devstack
          trigger-multiarch-image-build: true
          multiarch-suffixes: |
            -amd64
            -arm64
          full-setup: true

  # =============================================================================
  # Status: Report combined workflow status
  # =============================================================================
  workflow-status:
    name: Workflow Status
    needs: [create-manifest]
    if: always()
    runs-on:
      - non-api
      - X64
    steps:
      - name: Check for failures
        id: check
        run: |
          if [[ "${{ contains(needs.*.result, 'failure') }}" == "true" ]] || \
             [[ "${{ contains(needs.*.result, 'cancelled') }}" == "true" ]]; then
            echo "status=failure" >> $GITHUB_OUTPUT
          else
            echo "status=success" >> $GITHUB_OUTPUT
          fi

      - name: Update Status Check - Failure
        if: steps.check.outputs.status == 'failure'
        run: |
          echo "❌ Workflow failed"
          curl --fail -X POST \
            -H "Content-Type: application/json" \
            -H "Authorization: token ${{ github.token }}" \
            -d '{"state":"failure","context":"github/devstack-status-check","description":"Devstack Docker build and push failed","target_url":"https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"}' \
            "https://api.github.com/repos/${{ github.repository }}/statuses/${{ github.event.pull_request.head.sha || github.sha }}"
          exit 1

      - name: Update Status Check - Success
        if: steps.check.outputs.status == 'success'
        run: |
          echo "✅ Workflow succeeded"
          curl --fail -X POST \
            -H "Content-Type: application/json" \
            -H "Authorization: token ${{ github.token }}" \
            -d '{"state":"success","context":"github/devstack-status-check","description":"Devstack Docker build and push succeeded","target_url":"https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"}' \
            "https://api.github.com/repos/${{ github.repository }}/statuses/${{ github.event.pull_request.head.sha || github.sha }}"
          exit 0
